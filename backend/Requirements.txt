# DFECPIMS Backend Requirements
# Pin major versions for reproducibility; update minor versions as needed.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
asyncpg==0.30.0         # Async PostgreSQL driver for SQLAlchemy async engine
alembic==1.13.3          # Database migrations (optional if using init_schema.py)

# Auth
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4             # Password hashing
python-multipart==0.0.12           # Required for FastAPI file upload (Form, File)

# PDF generation
reportlab==4.2.2

# Utilities
python-dotenv==1.0.1     # Load .env files for local development
aiofiles==24.1.0         # Async file I/O for evidence storage
python-magic-bin==0.4.14     # MIME type detection from file bytes (not extension)

# Development / testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2            # Async HTTP client for FastAPI TestClient
